version: '3'
services:
  validator:
    build: .
    ports:
      - "6767:6767"
    command: develop
    volumes:
      - "./Validator:/validator/Validator"
networks:
  default:
    external:
      name: validator-network

